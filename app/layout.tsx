import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Premium Mangoes | Fresh Tropical Fruit Delivered",
  description: "Discover the finest quality mangoes delivered fresh to your door. Premium tropical fruit, rich flavor, and exceptional sweetness. Order now!",
  keywords: "mangoes, fresh mangoes, tropical fruit, premium mangoes, mango delivery",
  openGraph: {
    title: "Premium Mangoes | Fresh Tropical Fruit Delivered",
    description: "Discover the finest quality mangoes delivered fresh to your door.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="canonical" href="https://yourdomain.com" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
